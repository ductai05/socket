// main.cpp
#include <iostream>
#include "sendmail.h"

int main(){
	// client (request): 
    newMail(true, "dua xe", "");

    // server (response): 
    // newMail(false, "list process", "dandadan.png");
    return 0;
}